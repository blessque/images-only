# Changelog

| Iteration | Date | Summary |
|-----------|------|---------|
| 17 | 2026-08-19 | **The upload tray reported that photographs got 2–5x heavier, and it was half wrong and half right.** Caught on the test deploy: `906 KB → 5.7 MB`, `−-548%`. **The reporting half:** `compressedBytes` was the SUM of all four rungs, compared against one source file — but `srcset` hands a browser exactly one rung, so the sum is a weight no visitor has ever downloaded, and a real `526 KB → 240 KB` printed as `526 KB → 746 KB`. It is now the **largest rung**: the most any one visitor can be asked for, and the de-facto master since originals are not stored. Rejected the *typical* rung — the friendliest number, and a guess that depends on viewport and size class. The `−` was hardcoded in the markup, which is why a growth rendered `−-548%`; `savedLabel` signs it and a row that grew is drawn in the existing error red. **The real half, and worse:** a lossy WebP with transparency carries a **lossless alpha plane**, and it measured **2,963,776 bytes at quality 0.92, 0.80 and 0.62 alike — identical to the byte** — while the picture fell from 1.6 MB to 637 KB. The budget search was therefore chasing bytes it could not move: it saw a file 4x over, spent every step it had, **destroyed a megabyte of picture quality**, and finished still 4x over. The budget now governs `blob.size − alphaBytes(head)` — the honest form of "raise the budget for transparency", raising it by exactly the measured overhead rather than an invented multiplier. Opaque photographs are provably untouched: Chrome writes no `ALPH` chunk when every pixel is opaque, and four real photographs re-run through the worker produced **byte-identical ladders**. **Compositing onto black was offered and declined** — it is pixel-identical to what a black full-bleed page already shows and takes the same rung from 3.60 MB to 538 KB, but originals are not stored, so it is a one-way door on his photographs. The accepted consequence, recorded so it is not rediscovered as a bug: a soft alpha mask stays several times heavier than everything else and is now *slightly* heavier than before, because the pointless crush no longer happens. A clean cutout costs 3,096 bytes and is a non-issue. **A verifier that could not fail:** `parsePercent` stripped the sign, so the moment a growth could print `+20%` the "every compressed file shrinks" check would have read it as a 20% saving — fixed alongside. **The diagnosis ran on measurement, not inference:** the encoder was probed in real Chrome (quality *is* honoured; there is no PNG fallback; `q` omitted gives 4.9 MB, which is what a silent fallback would have looked like), and the tempting evidence in `export/` — whose sizes match today's *high-fidelity* numbers exactly — was **discarded as ten days stale** rather than read as a cause. One pre-existing failure is left standing and is not this change: four rows created 2026-08-09 in the *local* dev D1 advertise an `800` rung whose bytes are no longer in local KV, so `verify:admin`'s 404 check fails identically with and without this work. `npm run local:reset` clears it. 69 unit tests (13 new, covering the RIFF walk and the tray arithmetic); typecheck clean; main bundle unchanged. |
| 16 | 2026-08-19 | **The one-button deploy was pressed for the first time, on a clean account, and failed on the very first screen.** The Cloudflare dashboard rejected our config outright — *"There was a problem parsing the Wrangler configuration file"* — which made the pathway the whole handover rests on **100% broken for every new user**, while four of the five documented pathways went on describing it as the easy one. **The cause was a semicolon in a `package.json` script, and the error named a file that was never involved.** `local:reset` ran `wrangler d1 execute … --command "DROP TABLE IF EXISTS images; DROP TABLE IF EXISTS settings; …"`. The dashboard scans scripts for wrangler invocations and **splits them on `;` as shell separators**, ignoring that the semicolon sits inside a quoted argument; segment two comes back as `DROP TABLE IF EXISTS settings`, it fails to read that as a wrangler command, and reports *"There was a problem parsing the Wrangler configuration file"* — **the misdirection is the expensive half of the bug**, and it bought two wrong fixes before anyone doubted the config. Found by bisecting over **21 throwaway repositories** once static reasoning ran out. Both conditions are required and length is irrelevant: a **wrangler** invocation *and* a semicolon **with content after it** — one trailing `;` splits to an empty segment and is discarded, which is exactly why the near-identical `login:reset` always passed and hid the pattern. The SQL now lives in `scripts/reset-local.sql` and `scripts/reset-login.sql`, run with `--file` (**not** the `d1 execute --file` iteration 9 forbids — that rule is about migrations, where a re-run corrupts data; a teardown guarded with `IF EXISTS` is *meant* to be re-runnable, and both files say so in their headers). **The standing rule: no `package.json` script that mentions `wrangler` may contain a semicolon with anything after it.** Everything else was ruled out on the way and is recorded so nobody re-runs it — the config in both directions (ours in a bare 3-file repo passes; Cloudflare's own `saas-admin-template` config in our tree fails), comments and trailing commas (`x402-proxy-template` ships 112 comment lines *and* a trailing comma), every config key against the 37-template corpus, the dependencies, the lockfile, `.dev.vars.example`, the app source, and `docs/`. The `wrangler.jsonc` → `wrangler.json` rename was a **no-op aimed at the wrong file**. **Two mistakes worth keeping:** a corpus difference was shipped as a cause when it only narrows a search — only a retest closes one, and the retest was scheduled after the fix; and the first bisect was confounded by its own design, testing probes on `…/tree/<branch>` against a control that used a default branch, so "both fail" proved nothing. Every later probe used a fresh repository, after `raw.githubusercontent.com` was caught serving a five-minute-stale copy of the previous round. A bisect is only as good as its control. **Reading the button's path end to end then turned up a second, worse bug:** `npm run deploy` never applied migrations and nothing in `worker/` bootstraps the schema, so a new owner's deploy would have *succeeded* onto an empty D1 and failed on `no such table: images` — **iteration 12's exact failure, on the one pathway nobody had run** — while README and RUNBOOK both claimed migrations were applied. They now run in `deploy` and in CI, against the **binding** `DB` rather than the name `justimages`, because the new owner can rename the database on the setup screen; verified that wrangler resolves a binding there. `db:migrate:remote`, an unreferenced duplicate of `db:remote`, is deleted — two commands for one remote migration is the same *two owners for one schema* defect that caused iteration 12. The setup screen also now explains itself: `package.json` carries `cloudflare.bindings` descriptions, so the non-technical owner sees what `SETUP_CODE` is for instead of a bare input. Pushing then exposed a **third** dead mechanism: every run of `.github/workflows/deploy.yml` since it was committed has failed on `CLOUDFLARE_API_TOKEN` — the two repository secrets its own header comment documents **were never set** — so the CI deploy has never once run, and the live site has only ever been shipped by hand. **The lesson is one this log already recorded about `node/` and did not generalise: a pathway nobody has walked is a guess.** Three mechanisms here claimed to work on evidence nobody had gathered — the button (documented in three files, headlined in the README, never pressed), the CI deploy (red since day one), and the migrations the button was said to apply (never wired up). A green checkmark nobody has seen is a guess with better formatting. |
| 15 | 2026-08-10 | **The handover: the project had quietly acquired an operator, and nobody agreed to be one.** He asked for a page and expected a zip; what exists is a *service*, and a service has an operator — which by default was the author, permanently. Every mechanism meant to make the owner independent required a terminal: setup, backup, recovery, migration. `TUNING_LOG` promised "his photographs are his", but `npm run export` needs Node, so **for the person who owns the photographs that promise did not exist.** CLAUDE.md had the principle from day one — *the moment the workflow requires a terminal, the site stops being updated* — and had applied it only to uploads; essentially all of this iteration is that one sentence extended to the operator tasks. **(1) The site is claimed from a browser.** A fresh deployment ships unclaimed; `/api/setup` takes a password, hashes it, and stores it with a generated token secret in a new `auth` table. `credentials.ts` reads the database first and **falls back to the Worker secrets**, so the running deployment keeps working untouched. Guarded by `SETUP_CODE` — one word the owner invents, prompted for by the Deploy to Cloudflare button — because an unclaimed site hands admin to whoever asks first; it is used once and inert afterwards. `CHECK (id = 1)` makes claimed-once a **database invariant**, so two racing claims cannot both win however the handler is written. A separate table, not a `settings` row, because `readManifest` selects every settings row and a credential one refactor from the public manifest is the wrong place to keep it. Recovery is now `DELETE FROM auth` in the dashboard console — clicking, not `wrangler`, since the person who forgets the password is the person who cannot use a CLI. **(2) "Download everything"** in the admin bar: the same bytes and layout as `npm run export`, so `import` restores either. No dependency — a store-only ZIP writer, since the files are already compressed — accumulating **Blobs rather than one growing ArrayBuffer**, because a gallery is hundreds of megabytes and concatenating that can simply fail on a laptop. **(3) `npm run freeze`** turns a live gallery into plain files, verified by `verify-freeze.mjs` under `python3 -m http.server`: 18 images, nothing cropped, **CLS 0.00000 with no server at all**. **(4) The Node port is built, not asserted** — `OVERVIEW.md` had claimed "thirty lines of adapter" for two iterations. **Zero new dependencies despite having approval for one**: `node:sqlite` ships with Node 22.5+, so `better-sqlite3` was unnecessary, which also removes a native module that would have to compile on a 200₽ VPS. Proven by exporting 16 photographs from Workers KV and importing them into Node+SQLite+filesystem, byte-identical, size classes intact, `src/` and `worker/` unmodified. **(5) A fifth pathway the docs could not see.** The user asked why photographs cannot just be dropped in a folder by FTP, citing his own Aegea blog on Hostinger — and he was right; the prior answer ("no Cloudflare means he needs you forever") was wrong. **The dividing line is not Cloudflare vs not, it is server vs no server**, and every document framed the choice as "Cloudflare or leaving", which made shared hosting invisible: it is not leaving, it is a different kind of server. `php/index.php` does the Worker's job on any host with PHP 8 — the filename is the interface (`01-` orders, `-solo`/`-tight` set density, `site.txt` holds the footer), variants generated on first request and cached, ids including mtime so editing in place mints a new key and the immutable headers stay safe. The client is untouched. **Rejected or corrected along the way:** `better-sqlite3` (unnecessary once `node:sqlite` was checked); importing the old export (stock photographs, ChatGPT images and a real phone number — a byte-identical restore would have been a *successful* import and a *failed* outcome); video (a feature, not a hosting tier — 8MB upload cap, `<img>`-only grid); and `api.ts` reporting any non-OK response as "Incorrect password", which is what told the one person who knew the password, repeatedly, that he did not. **Foreign tools found what self-consistent tests could not, three times:** macOS `unzip` showed a mangled Cyrillic filename (the UTF-8 flag, bit 11, was never set) and a DOS timestamp of zero; a static file server exposed a mirror missing the admin chunk, because a dynamic import is named nowhere in the HTML and Vite spells asset paths three ways; and `fetch` **rejects** on a dropped connection, which the unlock dialog never caught — an unhandled rejection on any flaky network, not only a frozen copy. Two more assertions of mine failed and were mine again, the fifth and sixth times: 18-of-18 images before scrolling (lazy loading working correctly) and a stub payload for the duplicate-id check (rejected at validation long before uniqueness). Docs: `HANDOVER.md` for the author once, `/help` served from the owner's own site and linked **only from the admin bar** so the public page gains nothing, `docs/RUNBOOK.md` for whoever is next. 56 unit tests; worker, setup, node and freeze suites green; **main bundle unchanged at 199.55 kB / 63.46 kB gzipped** — all of it landed in the admin chunk (16.88 → 22.04 kB), which a visitor never downloads. |
| 14 | 2026-08-09 | **The site is live at `justimages.blessque.workers.dev` — and the first production login crashed the Worker for two hours while we blamed the password.** `PBKDF2_ITERATIONS` was 210,000 on OWASP's guidance; **the Workers runtime hard-caps PBKDF2 at 100,000** and throws `NotSupportedError` above it, so `/api/login` returned a 1101 edge exception and `api.ts:25` — which falls back to `'Incorrect password'` for any non-OK response — reported a crash as a wrong password. The password was never compared, not once. **Why the suite could not catch it is the useful half:** `auth.test.ts` sets `FAST = 1000` deliberately for speed and passes it explicitly to every password test, so the production constant was **never executed by any test**; Node's Web Crypto has no cap; and local `workerd` under `wrangler dev` does not enforce one. Unreachable by all three layers — unit, the 38 Worker checks, the 20 admin e2e — which is the same shape as iteration 2's CLS bug and iteration 3's routing bug, now the third confirmation that a green suite one level down says nothing about the level above. Pinned at 100,000 with an exported `PBKDF2_MAX_ITERATIONS` and the one assertion that would have caught it (40 unit tests). The security cost is real and accepted: below current OWASP, compensated by the limiter (8/15min), the 12-character minimum and a single user with no enumeration surface; Argon2/bcrypt would mean WASM at the edge, already settled against. **Regenerating the secret was mandatory, not hygiene** — `verifyPassword` reads the count out of the stored string, so an existing `pbkdf2$210000$…` keeps throwing after the code is fixed; the self-describing format makes raising the cost free but lowering it never is. **Three theories were wrong before the right one, and the diagnostic lesson outweighs all of them:** `wrangler tail` named the exception in ten seconds, and should have been reached for the moment the limiter showed attempts incrementing with no success — observable state was saying "requests arrive and fail" while we theorised about the one value the system is designed to never reveal. Recorded from the wreckage: **zsh expands `pbkdf2$210000$SALT$DIGEST` down to the literal `pbkdf2`** (`$210000` is positional parameter 210000, the rest are unset variables), which genuinely happened here and which `hash-password.ts:43` invites by printing the `.dev.vars` line in **double** quotes — single quotes required, and the ~80-vs-6 asterisk count at wrangler's prompt is the check. Pasting the script's whole output block into zsh also fed subsequent lines to whatever prompt was open, which is how an empty placeholder Worker got created and how Cloudflare's AI-agent skills got installed across seven tools, both answered "yes" by a hash. `TOKEN_SECRET` was echoed to the terminal in the process and was rotated — a signing key has no work factor between knowing it and forging a token, where the PBKDF2 hash beside it is built to survive exposure. **`npm run import` was considered and rejected**: `export/manifest.json` turned out to hold four Unsplash files, six ChatGPT images and MD5-shaped alt text — a test gallery, not the portfolio — plus a real phone number in `settings` that the import writes verbatim; a byte-identical restore would have been a *successful* import and a *failed* outcome, and re-uploading already-compressed variants is lossy-on-lossy against the 2400px master besides. The photographs go in by drag-and-drop, which is the product. First deploy: 7 assets, Worker 19.19 KiB / 5.57 KiB gzipped, bundle unchanged at 199.55 kB raw / 63.45 kB gzipped. |
| 13 | 2026-08-09 | **R2 turned out to require a credit card, so image bytes moved to Workers KV — and storage became pluggable.** KV needs no card (1 GB, 100k reads/day, 1,000 writes/day, free forever). The swap cost **3 lines**, because `worker/` and `src/` have shared types only since day one and all 7 mentions of R2 in `src/` were comments — that rule finally earned its keep. The user asked to fork the R2 build as a reference; **a fork over a 3-line difference would have been overhead that rotted**, so instead `worker/storage.ts` implements both backends and prefers R2 whenever its binding exists, the config block sits commented with the four steps to re-enable, and the commit is tagged `r2-reference` — switching back is configuration, not a merge. Details that mattered: KV has no ETag, so it is derived from the key, which is **truthful rather than a fudge** because keys are immutable by construction; upload size is now measured on the **actual bytes** instead of the client's `content-length` claim, a correctness win taken while passing; and `Tile.tsx` now retries three times on a backoff before showing the broken-image mark, because **KV is eventually consistent (~60s)** and a freshly published photograph can 404 for its own uploader — with `?r=n` only on retries, since a browser may cache the 404 and would otherwise answer from it. The tray warns when a batch exceeds the 1,000/day write budget rather than letting it fail part way through with no explanation. New `npm run export` / `npm run import` speak only to the public HTTP API, so they carried the gallery across backends and double as **data portability** — his photographs are his — and as the exit route to a Russian host, which `OVERVIEW.md` now costs out at a filesystem `storage.ts`, `better-sqlite3`, and a ~30-line adapter, with `src/` unchanged. Verified against KV: 39 unit tests, all three browser suites including the byte-identical round trip, and 200 images at **207 KB first load, LCP 528 ms, CLS 0.00000** — indistinguishable from the R2 numbers. |
| 12 | 2026-08-09 | **First deploy to real Cloudflare — and the schema turned out to have two owners.** `wrangler d1 migrations apply --remote` failed on `no such table: images`: `worker/schema.sql` CREATED the tables while `migrations/` only TRANSFORMED them, so every local database worked (schema.sql was always run first) and a fresh one — production, on day one — had nothing for 0001 to rebuild. **Two sources of truth for one schema was the real defect.** `worker/schema.sql` is deleted; `0000_initial.sql` reproduces the pre-0001 shape with `IF NOT EXISTS` throughout so it no-ops on older databases, and every database now reaches the current shape by one identical path. All four migrations then applied cleanly to production (D1 created in WEUR). Two incidents worth recording: filling the real `database_id` into the config made **miniflare open a new empty LOCAL database** — the old one sat untouched beside it under the placeholder id, but `COUNT(*) = 0` read exactly like data loss until the file was found and copied across with `ATTACH DATABASE`; an empty table after a config change is usually the wrong database, not a deleted one. And `wrangler login` hit a Cloudflare **bot challenge behind a VPN** (exit IP a Hetzner datacenter in Finland, Ray ID `…-HEL`) while `api.cloudflare.com` stayed healthy throughout — only the browser-facing handshake is challenged, and disconnecting the VPN fixed it. R2 still needs one manual enable in the dashboard (`code: 10042`) before buckets can be created. |
| 11 | 2026-08-09 | **Full class names on the per-tile bar, and `medium` renamed to `tight`.** `S W M` was a private code to memorise; the bar now reads `solo wide tight`, which costs about 60px and nothing else. The rename went through the **code and the database**, not just the button — leaving the UI saying "tight" while the data said "medium" is exactly the disagreement that made `big` confusing, and "medium" implied a size the class never controlled where "tight" says what it does (pack more per row). Migration 0003 rebuilds the table for the CHECK constraint; unlike 0001 it is idempotent in shape, since `tight` does not match `medium`. **The position number is gone** — it showed `2`, `3`, … at the end of the bar, but the grid already shows where an image is and the digit could not be acted on; `index`/`total` still reach the component, now only to disable the arrows at the ends. Verified visually as well as structurally: the bar renders `← → solo wide tight alt ×`. 39 unit tests, all three browser suites green. |
| 10 | 2026-08-09 | **Files under 150KB are uploaded untouched.** The user's point — there is no sense re-encoding a 52KB WebP — and the canvas API has **no lossless WebP mode** (`quality: 1` is still lossy), so any re-encode is a strict loss. A passthrough is stored once at `{id}/full.{format}` under its **own** extension and served with its true content type; `passthrough` and `format` ride on the manifest row (migration 0002), and the client emits **no srcset** because one object means `src` alone is the whole story. The image is still decoded, but only to read its dimensions, which the grid needs to reserve the tile before anything loads. It is a **pre-checked checkbox**, not an automatic rule: unchecking runs the ladder, because a 120KB PNG that would halve as WebP is still worth compressing and that stays the user's call — this is not the global skip-compression switch rejected in iteration 1, it is per image, reversible, and its default is right for the file in front of it. One checkbox now carries two meanings ("No compression" below the threshold, "High fidelity" above), which works because both mean *compress this less*. Passthrough rows show one number and the word "untouched" rather than a fake before/after. Two caveats recorded rather than hidden: the threshold is **bytes, not pixels** (a flat 4000px PNG can sit under it), and **EXIF survives** where the ladder strips GPS and camera serials for free. The e2e now proves the whole thing end to end — the object serves under `image/webp` and is **byte-identical to the source**. Also folded the pipeline dials into `compressParams.ts`, which CLAUDE.md has mandated since iteration 1. One assertion failed and was mine again, the fourth time: the shrinkage check swept up the passthrough row, asserting shrinkage over a file the feature exists to not shrink. 39 unit tests, all three browser suites green. |
| 9 | 2026-08-09 | **A migration corrupted local data, and the fix is that migrations are now tracked.** `npm run db:migrate` was `d1 execute --file`, which has no memory of having run. Migration 0001 maps `big→solo, medium→wide, small→medium` — so after one pass the old `small` rows are `medium`, and a second pass matches them on `WHEN 'medium' THEN 'wide'` and pushes them again. **`medium` is both an old name and a new one**, so the CASE cannot tell a migrated row from an unmigrated one; I had claimed it was idempotent by reasoning that untouched values fall through `ELSE`, which is wrong for exactly that value. The user ran it twice: 8 rows that should be `medium` became `wide`, unrecoverably — no column separates them from the 3 that were legitimately `wide`. The defect was never the SQL, it was that a migration was **re-runnable at all**. Now applied via `wrangler d1 migrations apply`, which records each file in `d1_migrations` and refuses to repeat it; `migrations_dir` moved inside the D1 binding (it is not a top-level wrangler field, and wrangler only warns about that). 0001 was marked applied on the existing database so it can never fire again — verified by re-running `db:migrate` and watching the class counts stay put. Second-order lesson recorded: "idempotent" is a claim to TEST, not to reason about; running it twice against a scratch database would have taken thirty seconds. |
| 8 | 2026-08-09 | **`solo` / `wide` / `medium` replace `big` / `medium` / `small` — the user hit the documented failure in his own photographs.** A `big` image rendered NARROWER than the `small` beside it. The mechanism, now pinned down: `big` *was* 1/1 and should have been alone, but a near-square image alone at 1440px solves to ~1440px tall, exceeding `MAX_ROW_HEIGHT` (1.4 × viewport) — so the clamp recruited a neighbour, and equal heights then locked widths to aspect ratios. **The height clamp was silently cancelling the only hierarchy mechanism the grid had.** His proposal, adopted: `solo` takes a whole row at any aspect ratio and is **exempt from the clamp**, which is the actual fix — without the exemption the clamp recruits a neighbour again. It is a legitimate exemption where the last row's was not, because solo is an explicit per-photograph choice rather than a silent degradation, so it is honoured literally and never warned about. The naming is also more honest than mine: "big" promised a size the grid could not deliver, "solo" promises to be alone. He dropped `narrow` as unnecessary — three classes, not four. Two rules the solver now enforces directly rather than through fractions: a solo image never joins a row in progress, and is never conscripted to fix another row's height — which means a shared row **can** exceed the ceiling when the only candidate is solo, a goal-not-guarantee of exactly the same shape as the `minRowHeight` floor, and the sweep test asserts the weaker true property instead of the convenient false one. **Shipped with a migration, not a reset**: SQLite cannot alter a CHECK constraint in place, so the table is rebuilt mapping big→solo, medium→wide, small→medium — verified on his own 17 uploaded photographs (6/3/8 → 6 solo / 3 wide / 8 medium, R2 untouched). Also: the upload tray now **drags to reorder** with larger arrows — only the handle is draggable, because making the row draggable breaks text selection in the alt input inside it, and the drop target is an insertion rule rather than a reflowing gap. One red run turned out to be test ordering, not a bug: `verify:worker` ends by deliberately tripping the login limiter, so `verify:all` now clears it before the admin suite. 37 unit tests and all three browser suites green. |
| 7 | 2026-08-09 | **A shipped bug, found by the user on his first real upload — and two UI gaps he was right about.** **(1) `srcset` advertised variants that were never written.** The encoder correctly refuses to upscale, so a 1024px source stops at the 800 rung — but `srcSetFor` listed all four unconditionally, so the browser picked 2400 at DPR 2, got a 404, and the tile fell through to the broken-image mark (18 × 404 on `2400.webp`, 8 × on `1600.webp`). **Why nothing caught it: every source in the entire test suite was ≥2400px**, so all four rungs always existed; the bug needs a source SMALLER than a rung, and AI-generated images at 1024×1024 hit it immediately. D1 now records `max_rung` — emitted rungs are always a prefix of the ladder, so one number describes the set — and it is covered three ways: a unit test on `availableRungs`, a 1024px source added to the e2e fixtures, and a blanket assertion that **no `/img/` request 404s** during the whole admin run. **(2) "High fidelity" was dead UI.** It rendered after compression had already run and only doubled the byte budget, which is a no-op for any photograph already under budget — measured 193 KB → 193 KB. It now re-encodes that one file and raises the starting *quality* (0.86 → 0.95, top rung 0.92 → 0.97), which is what the control has to mean: 193 KB → 274 KB. **(3) The tray now reorders before publishing** — `publish` walks the staged array and `sort_order` is assigned on insert, so the tray's order *is* the gallery's order, and arranging twenty photographs there beats shuffling them afterwards. One of my own assertions also failed and was wrong again: the colour check sampled `images[length - 1]`, which the new fixture and the reorder step moved, reporting a 187-channel drift that was pure test error — it now finds the swatch by alt text. Suite: 35 unit tests, and all three browser suites green. |
| 6 | 2026-08-09 | **Inter Tight, self-hosted.** The Google Fonts download ships TTF only — the woff2 files are served from their CDN and never appear in the ZIP, so the conversion was ours. Took the **variable** face rather than the 18 static ones (one file answers 100–900, so the footer's regular and the admin bar's bold share a single download; italic not shipped since nothing here is italic and it is a separate ~592KB face), and subset it to Latin + Cyrillic — the designer's name may well be Cyrillic, and the site's entire public text is one footer line. **567KB → 49.5KB, 91% smaller**, with the full 100–900 axis and 491 glyphs intact. The saving is the SUBSETTING, not the woff2 container: a straight TTF→woff2 lands near 200KB. Referenced by a relative path from `styles/fonts.css` so Vite fingerprints it into `/assets/` — a content-hashed name can be cached immutably where a stable name in `public/` must be revalidated every visit. `font-display: swap` is safe *here specifically* because the footer's height is fixed at 44px, so a late swap moves glyph widths inside a box that cannot change height — re-verified, **CLS still 0.00000** at all four breakpoints. Deliberately **not** preloaded: the font serves a footer at the end of the document and must not compete with the above-the-fold photographs. Confirmed in-browser that one file satisfies weights 100/400/700 and covers Cyrillic. Regeneration needs fonttools *and* brotli — the woff2 flavour fails without it; the command is recorded in `fonts.css`. |
| 5 | 2026-08-09 | **Phase 5 — editable footer, the last-row warning, and a measured perf budget.** The footer's name and contact become inputs **in place** in admin mode rather than opening a settings panel — they are the only copy on the site, and a panel would be more UI than content. The grid now surfaces the last-row problem where the user can act on it: when an incomplete final row solves past 1.5x the height clamp, admin mode shows advice ("add one more, or move a wider photograph to the end") **without changing the layout** — a gap and a crop are both hard-rule violations, so the row stays tall. **Measured against a real 200-image gallery** seeded into local D1/R2 (`npm run perf`): 200 tiles in 698 DOM nodes, **only 8 images fetched** on first load, **176 KB transferred** on a 1440x900 laptop (117 KB on mobile) split 4 KB HTML+manifest / 62 KB JS+CSS / 106 KB images, **LCP 492 ms**, **CLS 0.00000**. Two of my own instruments were wrong first and were fixed rather than reported: `outerHTML.length` measures the rendered DOM after React builds 200 tiles, not the served shell; and summing `content-length` headers silently attributed 100% of the payload to images, because wrangler serves JS and CSS chunked with no such header. `performance.getEntriesByType('resource')` is the honest instrument. The numbers settle the framework argument on evidence: JS is 62 KB of a 176 KB first load, and a visitor who scrolls the whole gallery will pull megabytes of photographs past it. Full suite green — 27 unit tests, 17 browser grid checks, 38 Worker checks, 20 admin end-to-end checks. |
| 4 | 2026-08-09 | **Phase 4 — admin mode and the upload pipeline, driven end to end in a real browser.** `Option+\\` or the footer lock opens a password field; the check happens on the Worker and the token lives in a plain JS variable, so a reload ends the session — verified. Drag-and-drop anywhere, or ⌘I. Files compress in a **Web Worker** through the four-rung WebP ladder with **progressive halving** before the final draw: a single `drawImage` from 6000px to 400px is a 15× reduction and the filter samples too sparsely at that ratio, so fine detail aliases into shimmer — which on a photography portfolio reads as a bad *photograph*, not a bad resize. Measured on real sources: **−80% and −69%** across the whole ladder. **Colour was measured rather than assumed**: `display-p3` canvas where the browser actually reports it back (passing the attribute is a request, not a guarantee — `getContextAttributes()` is the only honest test) plus `colorSpaceConversion: 'none'` on decode, giving `rgb(200,30,54)` in and `rgb(198,27,54)` out, a max channel drift of **3/255** which is WebP quantisation and not a colour-management error. The admin/public seam is a context in the main chunk holding no admin code, so `Grid`/`Tile` never import from `src/admin/` — and the e2e test **asserts a normal visitor's network log contains no AdminLayer or compressWorker request** rather than trusting the bundler. Chunks: AdminLayer 13.27 kB + 4.32 kB CSS, worker 1.87 kB, main bundle up only 2 kB. Also verified in one browser run: a wrong password shows an error and no admin chrome appears; alt text prefills from the filename (`Sunrise_over_the_bay.png` → "Sunrise over the bay") while a camera dump (`IMG_4821`) correctly yields **empty** rather than noise; three images publish and render at their true aspect ratio; the arrow icon reorders and the order persists server-side; delete raises an undo toast and undo genuinely restores, because the soft delete kept the R2 bytes. Two non-obvious things worth carrying: `Option+\\` must be matched on `event.code`, since Alt+Backslash on macOS produces `«` so `event.key` is never `\\`; and rungs above the source's long edge are skipped, because inventing a 2400px version of a 900px photo ships bytes carrying no information. One assertion of mine failed and **the test was wrong, not the code** — a solid-colour PNG is already near-optimally compressed, so ">50% for every file" was an invariant asserted over inputs it was never true for (the same mistake as Phase 2's `minRowHeight` floor). |
| 3 | 2026-08-09 | **Phase 3 — the Cloudflare Worker, integration-tested on local D1 and R2.** One Worker serves the static build *and* the API, which is what lets it inline the manifest into the HTML shell. R2 for bytes under immutable keys, D1 for the manifest, settings and the login-attempt counter. Auth is PBKDF2-SHA256 at 210k iterations via Web Crypto (native to Workers — bcrypt would mean shipping WASM to the edge for a single-user login), constant-time compare, and a compact HMAC token that is **deliberately not a JWT**: one issuer, one audience, one algorithm, so a header announcing which algorithm to trust would be pure attack surface. 14 more unit tests on the crypto, plus `npm run verify:worker` driving the real Worker end to end — **the integration test earned its keep immediately**. It found that the router returned the asset server's response whenever it was not a 404, and for `/` that is a **200 carrying the un-inlined index.html** — so `serveShell` never ran and the manifest injection, the entire architectural point, silently did nothing. No unit test would have caught it, because `serveShell` itself was correct. Fixed by keying on **content type** rather than on status or path: any `text/html` the asset server would return is replaced by the inlined shell. Verified in that same run: every one of the 7 write routes returns 401 with no token *and* with a forged one (asserted per route, not per middleware, because a route added outside a guarded group is a silent hole); a caption containing `</script>` cannot break out of the manifest block (`<` → `\\u003c`); variants round-trip through R2 byte-identical under `immutable`; a soft-deleted image leaves the manifest while its bytes survive so undo works; a colliding client-minted id returns **409, not a 500**; and the rate limiter cuts brute force at 9 attempts while refusing **even the correct password** during the window — refusing only wrong ones would make it a password oracle. Presigned R2 URLs were considered and dropped: a third secret to manage, to solve a problem (large uploads on the origin) that sub-megabyte rungs do not have. Worker bundles at 17.21 KiB / 5.00 KiB gzipped. Not yet deployed — needs a Cloudflare account, `wrangler d1 create` and two `wrangler secret put` calls. |
| 2 | 2026-08-09 | **Phase 2 — the grid solver, built and measured in a real browser.** Vite + React 19 + TypeScript 7 (strict, `noUncheckedIndexedAccess`). `solve.ts` is pure and DOM-free: greedy pack by target width fraction, then `H = W / Σaᵢ`, then satisfy the height clamp **by changing row membership** — pulling in or pushing out an image — rather than by cropping. 13 unit tests including a sweep of 60 images × every container width from 320 to 2560 in 7px steps. Three defects the tests caught before they shipped: **(1)** widths were derived from the exact height while the box reported a rounded one, so the box aspect drifted with the aspect ratio (~1.5px on a 3:1 panorama) — a crop, and "never crops" is the one promise. Rounding the height first turned out to be the wrong fix (it shifts Σ off the container by up to 0.5·Σa, forcing 2px width moves), so the height stays **fractional** and only widths are integers; rows stack in normal flow so fractional heights cannot seam. **(2)** the `minRowHeight` floor is unreachable for a single-image row — a lone panorama at 320px genuinely is 107px tall — so the *test* was overstated, not the code; it now asserts the solver breaches the floor only when it provably cannot do better. **(3)** `MAX_ROW_HEIGHT_VH` at 1.0 pulled a second image into every `big` row and so destroyed the grid's only hierarchy mechanism; raised to 1.4, which admits a lone full-width landscape while still catching portraits. **Browser verification (`npm run verify`, playwright-core + system Chrome) found a real bug the unit tests structurally could not**: CLS was 0.03–0.08 despite the docs claiming zero by construction. The first hypothesis — the dev-only async fixture fetch — was **wrong**; simulating the production inlined manifest barely moved it. Instrumenting individual `layout-shift` entries found ONE shift at 184ms attributed to `footer`: `Grid` seeded `containerWidth` at 0 and waited for `ResizeObserver`, so the first paint had no rows and the footer was then shoved down by 42 of them. Seeding from `document.documentElement.clientWidth` synchronously (not `innerWidth` — `clientWidth` excludes the scrollbar) took **CLS to 0.00000 at all four breakpoints**. Also fixed before shipping: `srcset` `w` descriptors must be the file's real pixel width, not the rung number, or variant selection is wrong for every portrait. 42 labelled fixtures generated with ffmpeg carry a border and centred caption so a crop would be visible, not merely measurable. Bundle: 195.77 kB raw / **61.99 kB gzipped** — worth recording because the earlier estimate of ~45KB was low, though it changes nothing about the decision (still roughly a quarter of one photograph). |
| 1 | 2026-08-09 | **Brainstorming and project framework.** Requirements settled, and three contradictions in the original brief resolved rather than papered over. **(1) Drag-and-drop upload vs. GitHub Pages** — Pages is static and has no process to receive a POST, so "where do images live" and "how does he upload" were never two questions. Resolved to a single **Cloudflare Worker** serving static assets *and* the API, with R2 for bytes and D1 for the manifest; $0/mo at this scale, zero egress, nothing to patch. **(2) A-paper cells vs. "never crop"** — the A-series has a fixed √2 aspect, which is exactly what cannot coexist with uncropped photos and no gaps; the user confirmed A-formats were an illustration of *nesting*, not a spec, and they were dropped entirely. Resolved to **justified rows**, `H = W / Σaᵢ`, the only algorithm satisfying never-crop + no-gaps + fills-width + flexible-height together. **(3) ffmpeg** — a CLI binary that cannot run in a browser, and the wrong tool for stills regardless: the dominant quality-per-byte win is not shipping 6000px into an 800px cell, which is a *resize* decision, not an encoder one. Replaced by native `OffscreenCanvas.convertToBlob` in a Web Worker, client-side before upload, which also makes his uploads ~16× smaller. **One non-obvious consequence surfaced and recorded**: within a justified row all images share a height, so widths are locked to aspect ratios — two same-aspect images render identically wide *regardless of size class*. Size class therefore controls row **density**, not size, and Big is `1/1` on desktop (a whole row) dropping to `1/2` on wide screens, which matches the user's own instinct for 2560px. React chosen over vanilla TS **on the roadmap, not on bytes** (forms, more pages, possibly a storefront); recorded explicitly so it is not re-litigated on bundle size. User decided **originals are not stored**, making the 2400px rung the de-facto master — so that rung alone is encoded at ~0.92 vs ~0.86, deliberate asymmetry as bought insurance against a one-way door. Rejected a global "skip compression" toggle as a footgun for a non-technical user, replaced by honest before/after byte counts plus a per-image escape hatch. User chose an opacity-pulse placeholder over the proposed ThumbHash, which **drops a dependency and an upload-time computation** — simpler than what was proposed. Framework copied from `kresti`: `CLAUDE.md`, `.claude/` (permissions + a `UserPromptSubmit` hook auto-injecting the tuning log on grid/compression/auth keywords), `docs/` (architecture, conventions, decisions), `memory/`, all gitignored per the same convention. No application code yet — deliberately, so the taste decisions in the grid could be settled first. |
