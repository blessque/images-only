node_modules/
dist/
.DS_Store
*.local
*.tsbuildinfo

# Local Claude working framework — guide, memory and settings, not part of the repo
CLAUDE.md
memory/
.claude/

# Cloudflare
.wrangler/
.dev.vars
.dev.vars.*

# ...except the template, which MUST be committed: the Deploy to Cloudflare button reads
# `.dev.vars.example` from the repository to know which secrets to prompt the new owner for.
# It contains names and placeholders only — never a value.
!.dev.vars.example

# Secrets
.env
.env.*

# Local test photos for grid work — never served, never built
/fixtures/

# Playwright / perf harness output
/test-results/
/playwright-report/
*.lighthouse.json

# Browser verification screenshots
/.screens/

# Gallery exports — your photographs, not source code
/export/

# Frozen static copies of the gallery — output, not source
/freeze/
